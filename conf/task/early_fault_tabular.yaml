name: early_fault_tabular
enabled: true
version: v1

task_type: binary_classification
input_mode: tabular

feature_source: cleaned
feature_columns:
  include: all
  include_patterns: []
  exclude_patterns: []
  exclude_columns: []

target:
  columns:
    - early_fault
  dtype: int64
  num_classes: 2
  class_names:
    0: normal
    1: abnormal

store:
  write_csv: true
  save_arrays: false

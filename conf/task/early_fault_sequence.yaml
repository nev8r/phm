name: early_fault_sequence
enabled: true
version: v1
cache_dir: null

task_type: binary_classification
input_mode: feature_sequence

feature_source: cleaned
feature_columns:
  include: all
  include_patterns: []
  exclude_patterns: []
  exclude_columns: []

sequence:
  length: 8
  stride: 1
  target_position: last
  drop_incomplete: true
  allow_cross_bearing: false
  allow_cross_split: false

target:
  columns:
    - early_fault
  dtype: int64
  num_classes: 2

metadata:
  keep_columns:
    - sample_uid
    - dataset
    - bearing_id
    - condition_id
    - source_group
    - sample_id
    - timestep

store:
  write_csv: true
  save_arrays: false
